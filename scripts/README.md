# Scripts

## `npm run seed`

Seeds the local SQLite runtime database from the JSON files in `data/`.

Use this after first install, or whenever you want to reset the local runtime DB back to the current seed set.
